/**
 * Global TypeScript type definitions shared across multiple features.
 * 
 * Types:
 * - ApiError: Standardized error response structure
 * - User: User account information (if implementing auth)
 * - PaginatedResponse<T>: Generic pagination wrapper (if needed)
 * 
 * Only types used by 2+ features should be defined here.
 * Feature-specific types belong in feature folders.
 */