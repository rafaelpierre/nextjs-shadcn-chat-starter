/**
 * Hook for handling navigation to chat routes.
 * Creates new conversation, stores initial message, and navigates to chat page.
 * 
 * @returns {Object} Navigation methods
 * @returns {(userMessage: string) => void} return.startNewConversation - Creates conversation and navigates to chat
 */