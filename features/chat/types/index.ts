/**
 * TypeScript type definitions for chat feature.
 * Defines interfaces for messages, conversations, API requests/responses.
 * 
 * Exports:
 * - Message: Individual chat message with role, content, timestamp
 * - Conversation: Full conversation with metadata
 * - ChatRequest: API request payload structure
 * - ChatResponse: API response payload structure
 * - StreamChunk: Structure for streaming response chunks (if using streaming)
 */