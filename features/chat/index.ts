/**
 * Public API barrel export for chat feature.
 * Controls what functionality is exposed to other parts of the application.
 * 
 * Exports:
 * - Types: Message, Conversation, ChatRequest, ChatResponse
 * - Hooks: useChat
 * - Components: MessageBubble, ChatInput (if needed externally)
 * 
 * Internal implementations (storage, API client) remain private to the feature.
 */